interface:
  display_name: "Cognetivy"
  short_description: "Repo-specific patterns and workflows for cognetivy"
  default_prompt: "Use the cognetivy repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true